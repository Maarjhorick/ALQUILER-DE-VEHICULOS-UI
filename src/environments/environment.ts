// Entorno de DESARROLLO. Se usa por defecto con `ng serve`.
// Si tu backend corre en otra URL o puerto, cambia solo este archivo.
export const environment = {
  production: false,
  apiUrl: 'http://localhost:8080/api'
};